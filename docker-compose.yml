services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: matcha_client
    restart: unless-stopped
    ports:
      - '5173:5173'
    volumes:
      - ./client:/app
      - /app/node_modules
    command: npm run dev -- --host
