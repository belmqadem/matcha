# Dependencies and package manager artifacts
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build outputs and coverage
dist
coverage

# VCS and editor metadata
.git
.gitignore
.vscode
.idea
.DS_Store

# Environment files (keep secrets out of build context)
.env
.env.*

# Test and docs artifacts
*.md

# Docker artifacts that are not needed in build context
Dockerfile*
docker-compose*.yml
